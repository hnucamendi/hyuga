import React from "react";

type AsideProps = {
};

const Aside: React.FC<AsideProps> = ({ }) => {
  return (
    <aside className="sidebar">
      <img />
      <span>Test</span>

    </aside>
  )
}

export default Aside
