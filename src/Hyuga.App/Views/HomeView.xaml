<UserControl x:Class="Hyuga.App.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,32">
            <TextBlock Text="Hyuga" FontSize="36" FontWeight="Bold" HorizontalAlignment="Center" />
            <TextBlock Text="Herramienta para pulido de machotes" FontSize="14" Foreground="{StaticResource TextMutedBrush}" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,24,0,0">
                <Button Content="Nuevo lote" Command="{Binding CreateNewBatchCommand}" Margin="0,0,12,0" />
                <Button Content="Abrir reciente" Command="{Binding OpenRecentCommand}" Background="{StaticResource SurfaceBrush}"
                        Foreground="{StaticResource TextPrimaryBrush}" BorderBrush="{StaticResource BorderBrush}" />
            </StackPanel>
            <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource AccentStrongBrush}" Margin="0,16,0,0" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#B23A2B" Margin="0,6,0,0" TextWrapping="Wrap" />
        </StackPanel>

        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1" CornerRadius="12" Padding="20">
            <StackPanel>
                <TextBlock Text="Proyectos recientes" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />
                <ListBox ItemsSource="{Binding RecentProjects}" SelectedItem="{Binding SelectedRecentProject}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding ProjectName}" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding ProjectPath}" FontSize="12" Foreground="{StaticResource TextMutedBrush}" />
                                </StackPanel>
                                <TextBlock Grid.Column="1" Text="{Binding LastOpenedAt, StringFormat=dd/MM/yyyy}"
                                           Foreground="{StaticResource TextMutedBrush}" Margin="12,0,0,0" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
