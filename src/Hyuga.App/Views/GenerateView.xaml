<UserControl x:Class="Hyuga.App.Views.GenerateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Generar PDF pulido" FontSize="24" FontWeight="SemiBold" />
            <TextBlock Text="Revisa el resumen y confirma la salida." Foreground="{StaticResource TextMutedBrush}" />
        </StackPanel>

        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1" CornerRadius="12" Padding="20">
            <StackPanel>
                <TextBlock Text="{Binding TotalPages, StringFormat=Total de paginas: {0}}" />
                <TextBlock Text="{Binding MissingFields, StringFormat=Campos pendientes: {0}}" Margin="0,6,0,0" />
                <TextBlock Text="{Binding OutputPath, StringFormat=Salida: {0}}" Margin="0,6,0,0" />

                <ProgressBar Value="{Binding ProgressValue}" Maximum="100" Height="18" Margin="0,20,0,0" />

                <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                    <Button Content="Volver" Command="{Binding BackCommand}" Background="{StaticResource SurfaceBrush}"
                            Foreground="{StaticResource TextPrimaryBrush}" BorderBrush="{StaticResource BorderBrush}" Margin="0,0,12,0" />
                    <Button Content="Generar" Command="{Binding GenerateCommand}" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
