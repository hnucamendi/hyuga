<Window x:Class="Hyuga.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hyuga" Height="640" Width="960" WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Hyuga" FontSize="28" FontWeight="SemiBold" />
            <TextBlock Text="Herramienta para pulido de machotes" FontSize="14" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Vertical" Margin="0,0,24,0">
                <Button Content="Crear nuevo lote" Height="44" Margin="0,0,0,12" />
                <Button Content="Abrir reciente" Height="36" />
            </StackPanel>

            <Border Grid.Column="1" BorderBrush="#D0D0D0" BorderThickness="1" CornerRadius="6" Padding="16">
                <StackPanel>
                    <TextBlock Text="Proyectos recientes" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />
                    <ListBox>
                        <ListBoxItem>Sin proyectos recientes</ListBoxItem>
                    </ListBox>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
